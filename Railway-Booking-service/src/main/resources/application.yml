server:
  port: 3333
spring:
  application:
    name: BOOKING-SERVICE
  data:
    mongodb:
      uri: mongodb+srv://lokesh:Loki@myfirstcluster.gxv9p.mongodb.net/ReserveTickets
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost